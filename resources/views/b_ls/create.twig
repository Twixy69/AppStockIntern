{% extends 'layout.app' %}

{% block content %}

	<div class="container">

	{% if errors.any() %}
		<ul>
			{% for errors in errors.all() %}
				<li> {{ error }} </li>
			{% endfor %}
		</ul>
	{% endif %}



	{{ form_open({'method': 'POST', url:route('b_l.store') }) }}

		<div class="form-group">
			{{ form_label('ref','Reférence BL') }}
			{{ form_text('ref', (b_ls.ref) ) }}

			{{ form_label('id_address','Adress') }}
			{{ form_select('id_address', (idAdress), null ) }}

			{{ form_label('matricule','Immatriculation') }}
			{{ form_text('matricule', (b_ls.matricule) ) }}
		</div>


		<div class="form-group">
		{{ form_submit('save', {'class':'btn btn-primary'}) }}
		</div>

	{{ form_close() }}


	</div>

{% endblock %}

{% extends 'layout.app' %}

{% block content %}

	<div class="container">

	{% if errors.any() %}
		<ul>
			{% for errors in errors.all() %}
				<li> {{ error }} </li>
			{% endfor %}
		</ul>
	{% endif %}



	{{ form_open({'method': 'POST', url:route('affaire.store') }) }}

		<div class="form-group">
			{{ form_label('ref_affaire','Reférence Affaire') }}
			{{ form_text('ref_affaire', (affaires.ref_affaire) ) }}
		</div>


		<div class="form-group">
		{{ form_submit('save', {'class':'btn btn-primary'}) }}
		</div>

	{{ form_close() }}


	</div>

{% endblock %}

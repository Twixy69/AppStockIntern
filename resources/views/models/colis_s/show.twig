{% extends 'layout.app' %}


{% block content %}

{{ debug() }}
Vue SHOW d'un colis.

    <h1 style="border-style: solid;
    border-color: {{ colis_s.color }}">{{ colis_s.number }}</h1>
    <p>Date d'emballage : {{ colis_s.boxing_date }}</p>
    <p>Date d'envoi : {{ colis_s.expedition_date }}</p>
    <p>Date livraison : {{ colis_s.reception_date }}</p>
    <p>Etat : {{ colis_s.state }}</p>
    <p>Poids : {{ colis_s.weight }}</p>
    <p>Créé le : {{ colis_s.created_at }}</p>
    <p>Mis à jour le : {{ colis_s.updated_at }}</p>
    <p>Créé par :{{ colis_s.created_by }}</p>
    <p>Mise à jour par :{{ colis_s.updated_by }}</p>
    <p>Adresse : {{ colis_s.id_address }}</p>


    <p> <a class="btn btn-primary" href="{{route('colis.edit',colis_s)}}"> EDIT</a> </p>

  	{{ form_open({'method': 'DELETE', url:route('colis.destroy',colis_s) }) }}
      {{ form_submit('Delete', {'class':'btn btn-danger'}) }}
  	{{ form_close() }}

    <table class="table table-hover">
      <thead>
        <tr>
          <th>Référence Piece</th>
          <th>Quantité</th>
          <th>Poids Unitaire</th>
        </tr>
      </thead>
      <tbody>
      {% for piece in pieces %}
        <tr>
          <td><a href="{{ route('piece.show',piece) }}">
            {{ piece.ref_piece }}
          </a></td>
          <td>{{ piece.pivot.quantity }}</td>
          <td>{{ piece.unit_weight }}</td>
        </tr>
      {% endfor %}
      </tbody>
    </table>


    <p> <a class="btn btn-primary" href="{{route('colis.index')}}"> Retour</a> </p>


{% endblock %}

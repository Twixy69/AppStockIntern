{% extends 'layout.app' %}


{% block content %}

	<div class="container">

	{% if errors.any() %}
		<ul>
			{% for errors in errors.all() %}
				<li> {{ error }} </li>
			{% endfor %}
		</ul>
	{% endif %}

	Ceci est la vue d'édition des pièces.


	{	{{ form_open({'method': 'POST', url:route('colis.update',[colis.id]) }) }}

			<div class="form-group">
				{{ form_label('number','Numéro colis') }}
				{{ form_text('number', (colis.number) ) }}

				{{ form_label('color','Couleur') }}
				{{ form_select('color', {'a':'b','c':'d'}) }}

				{{ form_label('id_adress','Adresse') }}
				{{ form_select('id_adress', (idAdress), (colis.id_address)) ) }}


			</div>


			<div class="form-group">
			{{ form_submit('save', {'class':'btn btn-primary'}) }}
			</div>

		{{ form_close() }}

	</div>
{% endblock %}

{% extends 'layout.app' %}


{% block content %}

	<div class="container">

	{% if errors.any() %}
		<ul>
			{% for errors in errors.all() %}
				<li> {{ error }} </li>
			{% endfor %}
		</ul>
	{% endif %}

	Ceci est la vue d'édition des colis.


	{	{{ form_open({'method': 'PUT', url:route('colis.update',[colis_s.id]) }) }}

			<div class="form-group">
				{{ form_label('number','Numéro colis') }}
				{{ form_text('number', (colis_s.number) ) }}

				{{ form_label('color','Couleur') }}
				{{ form_select('color', {'green' : 'vert',
				 												 'white' : 'BLANC',
																 'red' : 'ROUGE',
																 'blue' : 'BLEU',
																 'yellow' : 'JAUNE' }
												,(colis_s.color)
												)
				}}

				{{ form_label('id_adress','Adresse') }}
				{{ form_select('id_adress', (idAdress), (colis_s.id_address)) }}


			</div>


			<div class="form-group">
			{{ form_submit('save', {'class':'btn btn-primary'}) }}
			</div>

		{{ form_close() }}

	</div>
{% endblock %}
